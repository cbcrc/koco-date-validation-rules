{
  "name": "koco-date-validation-rules",
  "version": "1.2.0",
  "homepage": "https://github.com/cbcrc/koco-date-validation-rules",
  "authors": [
    "Mark Moyes <mark.moyes@cbc.ca>"
  ],
  "description": "Knockout validation rules for dates",
  "main": "src/date-validation-rules.js",
  "keywords": [
    "koco",
    "knockout",
    "validation",
    "dates"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "koco": "^1.0.0",
    "moment": "~2.13.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
